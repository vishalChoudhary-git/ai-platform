Why do Python packages have __init__.py?
"__init__.py marks a directory as a Python package. It enables clean imports and can also expose selected classes or functions from a package, similar to an index.ts file in a TypeScript project.
Although modern Python supports namespace packages without it, most production projects still include __init__.py for clarity and consistency."